{
  "name": "evolucao-parada-fria",
  "version": "1.0.0",
  "description": "Sistema de Gestão de Parada Fria",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend/dashboard && npm install",
    "build": "node render-build.js",
    "start": "cd backend && NODE_ENV=production npm start",
    "dev": "concurrently --prefix \"[{name}]\" --names \"backend,frontend\" --prefix-colors \"bgBlue.bold,bgMagenta.bold\" \"cd backend && npm run dev\" \"cd frontend/dashboard && npm start\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend/dashboard && npm start",
    "kill-ports": "powershell -Command \"Get-NetTCPConnection -LocalPort 3000,3001 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }\"",
    "clean-start": "npm run kill-ports && timeout /t 2 /nobreak >nul && npm run dev",
    "backend:build": "cd backend && npm run build",
    "backend:start": "cd backend && npm start",
    "frontend:build": "cd frontend/dashboard && npm run build",
    "frontend:install": "cd frontend/dashboard && npm install",
    "backend:install": "cd backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "keywords": [
    "react",
    "node",
    "typescript",
    "dashboard"
  ],
  "author": "Lucas-dev98",
  "license": "MIT"
}
